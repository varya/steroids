<div ng-app="<%= options.name %>App">

  <div ng-controller="ShowCtrl">
    <div class="content-padded">
      <h2>{{<%= options.name %>.name}}</h2>
      <p>{{<%= options.name %>.description}}</p>
    </div>

  </div>

</div>